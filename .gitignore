# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed: this ships as a binary application, so we want
# reproducible builds.

# Probe / scratch artifacts
*.html

# Local app data / runtime
*.db
*.db-wal
*.db-shm
*.log
